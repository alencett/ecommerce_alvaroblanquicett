import React from "react";
import Title from "./Title";

export const ItemListContainer = () => {
    return (
        < Title greeting="(nombre de usuario)"/>
    );
}

export default ItemListContainer;